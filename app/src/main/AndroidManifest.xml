<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="im.koala.bcsd">

    <application
        android:name=".KoalaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Koala.NoActionBar">
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsCollectionEnabled}" />
        <activity
            android:name=".ui.findpassword.FindPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.join.JoinActivity"
            android:exported="false" />
        <activity
            android:name=".ui.findid.FindIdActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.signup.SignupActivity"
            android:exported="true"
            android:label="@string/app_bar_title_signup"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
